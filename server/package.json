{
  "name": "roomy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small storage server for Roomy: files on disk, a JSON database, share links, a drop link for the mailbox, live sync and an optional Claude relay.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.4.0"
  }
}
