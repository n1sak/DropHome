{
  "name": "roomy",
  "version": "0.1.0",
  "private": true,
  "description": "Roomy: a house for your files. A spatial reimagining of cloud storage where rooms, furniture and drawers replace folders.",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix server install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "typecheck": "npm --prefix client run typecheck",
    "build:artifact": "node scripts/build-artifact.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
